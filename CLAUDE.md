# CLAUDE.md

## 작업 규칙

- 코드를 작성/수정하는 작업(기능 추가, 버그 수정, 리팩터링 등)을 완료할 때마다, 사용자에게 다시 묻지 않고 바로 `git add`와 `git commit`을 실행해 변경 사항을 커밋한다.
- 커밋 메시지는 변경 내용을 간결하게 한국어로 요약해서 작성한다.
- 단, 아래의 경우는 예외로 하고 커밋 전에 반드시 사용자에게 확인한다.
  - 원격 저장소로 `push`하는 경우
  - `git reset --hard`, `git checkout -- .` 등 기존 변경 사항을 되돌리거나 파괴하는 작업
  - `.env`, 인증 정보 등 민감한 정보가 포함될 수 있는 파일을 커밋하는 경우
